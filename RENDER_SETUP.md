# 🚀 Настройка Trading Core на Render

## ✅ Исправленные проблемы

### 1. Несоответствие имен переменных окружения
**Проблема:** Код искал `SUPABASE_KEY_FOR_CORE` и `BOTHOST_UI_API_URL`, но в Render были установлены другие имена.

**Исправление:**
- ✅ `SUPABASE_KEY_FOR_CORE` → `SUPABASE_SERVICE_ROLE_KEY`
- ✅ `BOTHOST_UI_API_URL` → `API_ENDPOINT`

### 2. Улучшенная диагностика
- ✅ Добавлена детальная проверка всех критических переменных при старте
- ✅ Конкретные сообщения об ошибках с именами отсутствующих переменных
- ✅ Вывод статуса всех переменных окружения в логи при запуске

---

## 📋 Обязательные переменные окружения для Render

Перейдите в настройки вашего сервиса на Render и добавьте следующие переменные в разделе **Environment Variables**:

### 1. Supabase (обязательно)
```
SUPABASE_URL=https://qdilspmiaoxrnotarjnq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFkaWxzcG1pYW94cm5vdGFyam5xIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTczMzc3ODMwMiwiZXhwIjoyMDQ5MzU0MzAyfQ.FhOJJb6-BOfLdJe5HQlR-DcMPsUmbnCyT3KQT0Yd7Gs
```

### 2. Шифрование (обязательно)
```
ENCRYPTION_KEY=<ваш_ключ_шифрования_Fernet>
```

### 3. Telegram Admin Bot (обязательно)
```
TELEGRAM_BOT_TOKEN_ADMIN=<токен_вашего_admin_бота>
ADMIN_CHAT_ID=<ваш_telegram_chat_id>
```
⚠️ **ВАЖНО:** Замените `<ВАШ_ЛИЧНЫЙ_CHAT_ID>` на реальный ID!

### 4. API Endpoint (обязательно)
```
API_ENDPOINT=<URL_вашего_UI_бота_на_Bothost>
```

### 5. Опциональные параметры
```
ANALYSIS_INTERVAL=10
DEFAULT_ASSET=EURUSD=X
```

---

## 🔍 Проверка после развертывания

После развертывания на Render проверьте логи. Вы должны увидеть:

```
============================================================
🚀 Trading Core Starting...
============================================================
Статус переменных окружения:
  SUPABASE_URL: ✅
  SUPABASE_SERVICE_ROLE_KEY: ✅
  ANALYSIS_INTERVAL: ✅ (10s)
  DEFAULT_ASSET: ✅ (EURUSD=X)
============================================================
✅ Supabase клиент успешно инициализирован: https://qdilspmiaoxrnotarjnq.supabase.co
Core starting up...
```

---

## ❌ Что делать, если видите ошибки

### Ошибка: "Критические переменные окружения не установлены"
```
🚫 Критические переменные окружения не установлены: SUPABASE_SERVICE_ROLE_KEY
```
**Решение:** Добавьте отсутствующую переменную в настройках Render Environment Variables.

### Ошибка: "Переменная API_ENDPOINT не задана"
```
🚫 Переменная API_ENDPOINT не задана в настройках окружения!
```
**Решение:** Добавьте `API_ENDPOINT` с URL вашего UI-бота на Bothost.

### Ошибка: "Ошибка при создании Supabase клиента"
```
❌ Ошибка при создании Supabase клиента: ...
```
**Решение:** Проверьте правильность `SUPABASE_URL` и `SUPABASE_SERVICE_ROLE_KEY`.

---

## 🔄 Повторное развертывание

После добавления всех переменных окружения:

1. **Сохраните изменения** в настройках Render
2. **Нажмите "Manual Deploy"** → "Deploy latest commit"
3. **Проверьте логи** на наличие успешного запуска
4. **Статус должен измениться** с "Deploy failed" на "Live"

---

## 📝 Checklist перед развертыванием

- [ ] Все 6 обязательных переменных добавлены в Render
- [ ] `ADMIN_CHAT_ID` содержит реальный Telegram ID (не заглушку)
- [ ] `SUPABASE_URL` начинается с `https://`
- [ ] `SUPABASE_SERVICE_ROLE_KEY` - это service_role ключ (не anon)
- [ ] `API_ENDPOINT` указывает на работающий UI-бот
- [ ] Изменения закоммичены и отправлены в репозиторий

---

## 🎯 Итоги исправлений

Все критические проблемы устранены:
1. ✅ Исправлены имена переменных окружения в коде
2. ✅ Добавлена детальная диагностика при старте
3. ✅ Улучшены сообщения об ошибках
4. ✅ Создана документация по настройке

**Следующий шаг:** Закоммитьте изменения и разверните на Render!
